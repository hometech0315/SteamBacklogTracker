<div class="nav-container">
    <div class="nav-header">
        <MudLink Href="/" Class="nav-brand">
            <div class="nav-brand-icon">
                🎮
            </div>
            <div class="nav-brand-text">Steam Backlog</div>
        </MudLink>
    </div>
    
    <MudNavMenu Class="nav-menu">
        <div class="nav-section">
            <div class="nav-section-title">Library</div>
            <MudNavLink Href="/" Match="NavLinkMatch.All" Icon="@Icons.Material.Filled.Home" Class="nav-link">
                Dashboard
            </MudNavLink>
            <MudNavLink Href="/games" Icon="@Icons.Material.Filled.Games" Class="nav-link">
                <div class="nav-link-content">
                    <span class="nav-link-text">Game Library</span>
                    <span class="nav-badge secondary">42</span>
                </div>
            </MudNavLink>
            <MudNavLink Href="/achievements" Icon="@Icons.Material.Filled.EmojiEvents" Class="nav-link">
                <div class="nav-link-content">
                    <span class="nav-link-text">Achievements</span>
                    <span class="nav-badge warning">156</span>
                </div>
            </MudNavLink>
        </div>
        
        <div class="nav-section">
            <div class="nav-section-title">Analytics</div>
            <MudNavLink Href="/statistics" Icon="@Icons.Material.Filled.Analytics" Class="nav-link">
                Statistics
            </MudNavLink>
            <MudNavLink Href="/progress" Icon="@Icons.Material.Filled.TrendingUp" Class="nav-link">
                Progress Tracking
            </MudNavLink>
        </div>
        
        <div class="nav-section">
            <div class="nav-section-title">Management</div>
            <MudNavLink Href="/sync" Icon="@Icons.Material.Filled.Sync" Class="nav-link">
                Sync Steam
            </MudNavLink>
            <MudNavLink Href="/settings" Icon="@Icons.Material.Filled.Settings" Class="nav-link">
                Settings
            </MudNavLink>
        </div>
    </MudNavMenu>
    
    <div class="nav-footer">
        <div class="nav-user">
            <div class="nav-user-avatar">
                SG
            </div>
            <div class="nav-user-info">
                <div class="nav-user-name">Steam Gamer</div>
                <div class="nav-user-status">Online</div>
            </div>
        </div>
    </div>
</div>

<style>
    /* Override MudBlazor drawer styles for gaming theme */
    .mud-drawer {
        background: linear-gradient(180deg, var(--gaming-surface) 0%, var(--gaming-background) 100%) !important;
        border-right: 1px solid rgba(255, 255, 255, 0.1) !important;
    }
    
    .mud-navmenu {
        background: transparent !important;
    }
    
    .mud-nav-link {
        color: var(--text-secondary) !important;
        border-radius: 0 !important;
        margin-bottom: 0.25rem;
        position: relative;
    }
    
    .mud-nav-link::before {
        content: '';
        position: absolute;
        left: 0;
        top: 0;
        bottom: 0;
        width: 4px;
        background: var(--steam-gradient);
        transform: scaleY(0);
        transition: transform 0.2s ease;
    }
    
    .mud-nav-link:hover {
        background: rgba(255, 255, 255, 0.05) !important;
        color: var(--text-primary) !important;
    }
    
    .mud-nav-link:hover::before {
        transform: scaleY(1);
    }
    
    .mud-nav-link.mud-nav-link-active {
        background: rgba(30, 136, 229, 0.15) !important;
        color: var(--steam-blue-light) !important;
    }
    
    .mud-nav-link.mud-nav-link-active::before {
        transform: scaleY(1);
    }
    
    .mud-nav-link.mud-nav-link-active::after {
        content: '';
        position: absolute;
        right: 1rem;
        top: 50%;
        transform: translateY(-50%);
        width: 6px;
        height: 6px;
        background: var(--steam-blue);
        border-radius: 50%;
        animation: activePulse 2s ease-in-out infinite;
    }
    
    .mud-nav-link .mud-nav-link-icon {
        color: inherit !important;
    }
    
    .mud-nav-link .mud-nav-link-text {
        color: inherit !important;
    }
    
    /* Gaming theme scrollbar */
    .mud-drawer .mud-drawer-content::-webkit-scrollbar {
        width: 4px;
    }
    
    .mud-drawer .mud-drawer-content::-webkit-scrollbar-track {
        background: transparent;
    }
    
    .mud-drawer .mud-drawer-content::-webkit-scrollbar-thumb {
        background: rgba(255, 255, 255, 0.2);
        border-radius: 2px;
    }
    
    .mud-drawer .mud-drawer-content::-webkit-scrollbar-thumb:hover {
        background: rgba(255, 255, 255, 0.3);
    }
    
    /* Navigation link content and badges */
    .nav-link-content {
        display: flex;
        align-items: center;
        justify-content: space-between;
        width: 100%;
    }
    
    .nav-link-text {
        flex: 1;
    }
    
    .nav-badge {
        background: var(--achievement-gradient);
        color: var(--gaming-background);
        font-size: 0.75rem;
        font-weight: 600;
        padding: 0.125rem 0.375rem;
        border-radius: 10px;
        min-width: 20px;
        text-align: center;
        animation: badgePulse 2s ease-in-out infinite;
        margin-left: 0.5rem;
    }
    
    .nav-badge.secondary {
        background: linear-gradient(135deg, var(--steam-blue) 0%, var(--steam-blue-dark) 100%);
        color: white;
    }
    
    .nav-badge.warning {
        background: var(--achievement-gradient);
        color: var(--gaming-background);
    }
    
    @@keyframes badgePulse {
        0%, 100% { transform: scale(1); }
        50% { transform: scale(1.1); }
    }
</style>
